import React from "react";

const Loading = () => {
  return (
    <div
      className={
        "flex h-screen w-screen items-center justify-center text-center"
      }
    >
      Loading...
    </div>
  );
};

export default Loading;
